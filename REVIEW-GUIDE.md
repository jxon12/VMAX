# V-MAX — 本轮检查与演示指南

更新：2026-09-08。目标是把「替默认组织者分担持续协调」讲清楚，而不是用功能数量承诺评分。

**线上原型：** [vmax-one.vercel.app](https://vmax-one.vercel.app/)。已在 2026-09-08 验证无需登录即可打开，并显示当前 Hey V-MAX 行动工作区。现有前端托管于 Vercel，旅程资料仍保存在当前浏览器；这不代表真实后端、身份认证或 AI 服务已经接通。

## 全站 Lens 界面更新

- Home 沿用用户选择的 Fuji 竖照，封面与内容分层；Trip updates → Next up → Trip wallet 的顺序与数据保留。纸质票夹继续保留，照片上的操作与导航采用清透边缘。
- Journeys 为照片主导的收藏式列表；Profile 收拢为个人 Agent、旅程上下文和可展开的 **Privacy & Agent permissions**，没有更改任何共享授权。Saved 与 Discover 使用同一摄影卡片。
- Discover 去掉拼贴／假登机牌装饰，保留搜索和筛选；This season 保留向左下一张、向右上一张与收藏。详情页将规划操作提前，阅读说明使用开放排版。
- Planner 保留描述、目的地选择、设置和 Journey Pass 四步。日期、人数与预算直接可见；舒适度和饮食需求收进 **Your kind of journey** 展开区。
- V-MAX 使用放大的 Orb、明确的待审建议和辅助操作；文字输入由 **Type instead** 打开并获得焦点。介绍页展示 Compare → Coordinate → Carry out，并说明只把已批准的结果写入 Plan / Wallet，而非模拟视频播放器。语音仍是演示，没有打开麦克风。
- 全站统一浮动玻璃底部导航、清晰的弹层、字体层级、按压反馈；新增设计不重置任何行程、费用、聊天或保存资料。支持减少动态／透明度偏好。

## Journey 界面更新

- Overview 使用铺满顶部的真实目的地照片。含 Fuji 行程时与首页使用同一照片来源，标题为 Tokyo. Then Fuji.；下一站悬浮在照片上，待确认事项成为简洁提示，下方 Within reach 提供证件、清单和预算。
- 封面有有界的指针／滚动视差；关闭动态效果时停用。Journey 导航可点击、水平拖动，或用方向键／Home／End 切换，选中镜片平滑移动；没有劫持页面滚动。
- Plan 使用日期胶囊与开放时间线，照片、标题和时间优先；排序控制只在 Reorder 模式显示，Agent 建议点开后才展示完整审批信息。
- Group 以聊天记录和输入框为主。输入 `@` 选择个人 Agent；右侧待审数量打开 Shared decisions。
- Route 将下一站与交通入口合进地图；Budget 聚焦剩余预算、费用和每人应收／应付，明细解释可展开。
- 顶部导航安装并锁定 simple-liquid-glass 4.1.0（MIT，无额外运行时依赖），只使用一个低质量档位的 rim 折射层，文字在滤镜外保持锐利。不是原生系统 Liquid Glass：Chromium 使用 SVG 背景折射；Safari／Firefox 使用组件降级效果。减少透明度时改为实底。
- 底部导航和照片上的操作使用低模糊透明边缘；详细阅读弹层仍保留足够的底色。内页改为中性炭灰背景与照片透色，减少旧版不透明蓝紫卡片。
- 演示章节切换收进右上方 **Demo · Before departure** 按钮，不再常驻两张章节卡。原有行程、聊天和费用数据不清空。

## 核心叙事

小团体出游时，计划完成不代表组织工作结束。某个人仍不断回答「下一站去哪」「他几点结束」「我们怎么集合」，并处理临时变动。

V-MAX 的主线：每人掌握自己的共享边界；个人 Agent 只回答获准的信息；协调者提出可检查的共同安排；必要的成员确认之后，同一份 Journey 才更新。

这个 prototype 展示此交互和状态流，不代表已经运行真实的多 Agent 网络。第二个 Agent 也不是事实保证：来源、约束检查、成员权限、人工确认和可追溯结果仍然必要。

## 建议检查顺序

1. **Profile → Privacy & Agent permissions**
   - 调整自己的空闲时间、大致区域、每餐预算及共享开关。
   - 未保存时是预览；保存后群聊和待确认方案使用相同设置。
   - 关闭预算共享，自己的 Agent 不应再透露预算。
   - 只有三项共享均开启，才能授权符合范围的晚餐集合。关闭任一项会取消此授权。授权不包含付款或预订。

2. **Home → 博物馆更新 → 审阅**
   - 来源入口、原计划问题、替代日期和预算估算都可检查。
   - 按 **Confirm my part**：只记录自己的同意，行程还不改变。
   - **Preview member approvals · DEMO**：显式模拟其他成员同意，不发送真实消息。
   - 全员同意后，原来的博物馆条目移动到空闲日期，而不是添加重复条目。默认是 Day 4 的 11:30；若行程被改过，可能需要其他空闲日期或人工处理。
   - Home、Plan、Route、群聊记录共同反映改动。

3. **Journey → Demo → Day 2 in Tokyo → Group**
   - 在输入框键入 `@`，可选择 V-MAX 或任一成员的 Agent。
   - 例如：`@Joshua’s Agent, when are you free to meet for dinner?`
   - 回复使用示例共享信息，不擅自公开详细工作地址。
   - 点消息中的方案入口、顶部待审数量，或输入框上方的 **Find a time to reunite**。
   - 默认示例中 18:30 不够 Joshua 转场，按钮会禁用并解释原因；19:00 满足已共享约束。
   - 其他成员若未授权，需要他们单独确认；自己的设置也可能使方案等待或阻塞。
   - 确认后，晚餐真实写入本地行程，Overview 和 Route 的下一站随之更新。

4. **Plan / Route → 检查结果与控制权**
   - 白天示例探索活动由三人参与，晚餐是四人重聚；其他人的私人会议不被放进你的路线。
   - 时间冲突按实际参与成员判断；不同成员同一时间各有活动，不会被误判成整个小组冲突。较轻松的行程调整也保留参与人。
   - Route 按真实保存的行程显示下一站；切换查看日期不等于改变当前演示时间。
   - 可以手动标记到访并撤销。交通入口只打开路线或服务选择，没有自动叫车。
   - 方案支持 Undo；如果确认后的站点已被手动编辑或移除，Undo 暂停，避免覆盖用户后续修改。
   - 待审博物馆方案不能覆盖已完成、改名、改价或更换参与人的原条目；不参与的人不能代为确认。撤销恢复旧时间若会撞上新活动，也会暂停。

5. **Budget → 收据份额**
   - 示例 Pizza 为 MYR 64，份额 `1 : 2 : 1 : 0`；Water 为 MYR 4，份额 `1 : 0 : 1 : 0`。
   - 结果是 You 18、Louise 32、Simyee 18、Joshua 0，共 MYR 68。
   - 项目、金额、付款人、份额都可改；小数以分计算并处理余数。
   - 保存后更新费用和余额；再次编辑替换同一张收据，不重复累加。

## 不超过 5 分钟的展示取舍

最终只提交公开 GitHub 和 unlisted YouTube 链接。**Ideation 的 25% 只通过 README 评分**；mindmap、迭代取舍、真实 mentor 反馈及改动证据要放在 README，不能依靠视频里的介绍替代。视频目标 4:40，留出五分钟上限的余量。

- **0:00–0:30**：具体目标用户与组织者反复被问「下一站」的痛点。用真实经历或访谈，不扩大成所有旅行者的结论。
- **0:30–1:00**：Home 与 Hey V-MAX 行动工作区，展示可检查的工作，不只展示聊天或开场动画。
- **1:00–1:55**：闭馆演示的来源、建议、自己的确认、明确标记的模拟成员同意，以及实际更新后的 Plan。
- **1:55–3:25**：切 Day 2；群聊询问个人 Agent，展示不共享的信息、18:30 的阻塞、19:00 的重聚，再查看 Plan / Route 一致的结果。
- **3:25–3:55**：收据份额和精确结果，作为短补充。
- **3:55–4:25**：现有架构与模拟边界；真实身份、来源和同步是后续工作。
- **4:25–4:40**：真实测试发现；尚未测试就说下一步验证计划，不展示未经测量的「节省 X% 时间」。

为了镜头清楚，可先演示 Before departure 的闭馆处理，再切 Day 2。两个章节只是改变固定时钟，不清空已保存的决定或聊天。复演时使用对应方案的 Undo；不要清理浏览器存储来覆盖现有用户数据。

## 评分维度对应的可展示证据

| 维度 | 本轮可展示 | 提交前仍需补充的真实证据 |
| --- | --- | --- |
| Impact · 20% | 默认组织者的反复问答、变动协调与等待被同一流程承接 | 目标用户访谈；使用前后相同任务的时间、来回追问次数、完成情况 |
| Ideation · 25% | 旧版静态 Journey 到权限、共享决定、持续协调的变化 | 原始 mindmap、mentor 反馈与改动对应关系；不能伪造反馈 |
| Design · 10% | 保留首页视觉；清晰的主操作、审批结果、个人权限、可编辑份额 | 真实用户试用中的可理解性和操作问题 |
| Creativity · 15% | 有边界的个人 Agent + 小组重聚 + 确认后统一更新 | 与实际现有方案的功能对比，避免声称「首创」而没有证据 |
| Feasibility · 15% | 单一 Trip 状态、明确的数据字段、约束校验、幂等更新及回归测试 | 登录/成员身份、服务端权限、并发版本、真实数据源、成本与接入计划 |
| Presentation · 15% | 可复演的两个章节；来源和模拟边界可见 | 符合最终提交模板、录屏清晰、有声、控制在要求时长 |

## 实现与模拟的边界

**现在可在本地操作：** 个人 Agent 设置、多个 @ 选择与定向规则回复、约束/权限检查、逐成员确认、方案应用与安全撤销、跨页统一行程、日期浏览、费用/份额计算、保存后刷新恢复。

**目前为模拟或参考：** Agent 智能回复、成员同意、固定演示时钟、闭馆发现、机场照片指引、地图背景、样例票券与语音。没有实时监控、真实日历、跨设备同步、认证、持续定位、摄像头 OCR、在线多人电话、真实出票或支付。

**现实实现顺序：** 先加入身份认证与成员级服务端权限、共享行程版本和审计记录；再接入有时间戳的官方/运营商数据并要求来源检查；随后接入语音和收据识别（识别结果先供用户纠错）；最后在明确授权、重新核价和最终确认之后考虑外部预订。不得把本地规则开关当成真实数据安全措施。

**拟定试点架构（后端尚未接入）：** 保留已经部署的 Vercel 静态前端，后续拟用 Supabase Auth、Postgres/RLS、Realtime 和 TypeScript Edge Functions 承接真实成员与共享状态；方案通过一次数据库事务复核后应用。个人 Agent 先作为同一协调器内的权限受限角色，不承诺已运行 A2A 网络。无需为了此方案迁移到 Cloudflare；[构建与提交补充说明](docs/BUILD-AND-SUBMISSION-NOTES.md) 中的 Cloudflare 额度仅是备选托管参考，不是当前 Vercel 的账单。Vercel 账户方案及试点费用仍需确认，Supabase、模型、语音和旅行供应商费用也不是已开通的服务成本。

## 验证记录

- 生产构建通过。
- 78/78 项回归测试通过，覆盖目的地隔离、日期与预算、参与人独立时间线、过期方案保护、幂等改期/重聚、安全撤销、权限撤回、多个 @ 问题隔离、无权限拒答、精确分账，以及本地 Profile 与模拟机票比较。未预订 shortlist 不会增加待补票券或准备事项中的证件计数。最终提交前若再有改动，需要重新测试。
- 浏览器检查了个人设置、@ 列表、等待与确认、改期后的实际条目、Overview/Route 下一站、首页更新、V-MAX 跳转、收据保存与重新编辑，以及 390px / 320px 布局。
- 本轮还验证了本地 Profile 输入后刷新保留、比较方案确认后在 Trip wallet 保留，以及群聊 18:30 阻塞、19:00 可确认的流程。
- 线上 Vercel 原型已验证无需认证即可打开当前 Hey V-MAX 工作区；此项只确认公开访问与页面版本，不代表所有线上流程均已重测。最终提交前，应在该网址完整复演一次。
- 这些是实现验证，不是目标用户研究，也不等于后端或外部服务已经上线。
