# README screenshot provenance

Captured from the public frontend at https://vmax-one.vercel.app/ on 8 September 2026, using the browser's displayed UI. These are real interface captures, not generated mockups.

| Screen image | Visible state |
| --- | --- |
| `home-screen.png` | Home: Fuji cover, journey entry and Trip updates heading |
| `hey-vmax-screen.png` | Hey V-MAX task workspace: flight comparison and coordination entry points |
| `shared-decision-screen.png` | Reunion review: 18:30 conflicts with Joshua's modeled 18:50 arrival; confirmation disabled |
| `receipt-screen.png` | Unsaved sample receipt: MYR18 / 32 / 18 / 0, total MYR68 |

The accompanying `.jpg` files are the original 1280 × 720 browser captures. The `-screen.png` versions remove only the empty desktop side margins at the app's observed bounds (x=435, width=410). No UI, labels or results were redrawn. The images show visible portions, not every scrollable section.

All travellers, replies, schedules and receipt amounts shown are sample data. No reservation, payment or message to another person was made to capture these screens. App screenshots do not establish licensing rights for underlying photography or other third-party assets; the asset-rights review remains on the submission checklist.
